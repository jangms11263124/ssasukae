import { TickerFooter } from '@/shared/ui/ticker/TickerFooter';

export function AdminTickerFooter() {
  return (
    <TickerFooter
      label="[ADMIN_CONSOLE]"
      message="SYSTEM_ACCESS : AUTHENTICATED : ENCRYPTION_ACTIVE : SYNC_MODE_ENABLED : DISK_SPINNING"
    />
  );
}
