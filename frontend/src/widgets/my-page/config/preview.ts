/**
 * 마이페이지 미리보기 목록(찜·최근 활동)의 최대 표시 개수.
 *
 * 서버(GET /api/users/me/mypage)도 두 목록을 최근 3개로 잘라 내려주지만,
 * 응답 계약이 바뀌어도 카드가 항목 수만큼 무한정 길어지지 않도록
 * 프론트에서도 같은 값으로 방어한다.
 */
export const MYPAGE_PREVIEW_COUNT = 3;
